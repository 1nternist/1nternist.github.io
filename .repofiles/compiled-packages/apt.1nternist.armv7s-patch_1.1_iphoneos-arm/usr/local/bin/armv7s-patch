#!/bin/sh

#  armv7s
#  ARMv6-on-A6
#
#  Created by rio@sident.net on 13/06/03.
#

set -e

ARM2ARMV7S="/usr/local/bin/arm2armv7s"

test $# -eq 1

cp -p "$1" "$1.new"
${ARM2ARMV7S} "$1" "$1.new"
/usr/bin/ldid -S "$1.new"

test -e "$1.org" || mv "$1" "$1.org"
mv "$1.new" "$1"
